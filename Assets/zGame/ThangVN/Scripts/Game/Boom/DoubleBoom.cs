using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DoubleBoom : IBoom
{
    public override void Init(Vector3 spawnPos)
    {
        base.Init(spawnPos);
    }
}
